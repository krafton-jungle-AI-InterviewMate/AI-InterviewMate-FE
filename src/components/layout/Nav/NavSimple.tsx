import Logo from "../common/Logo";
import Nav from "./Nav";
import ProfileButton from "../common/ProfileButton";

const NavSimple = () => {
  return (
    <Nav>
      <Logo />
      <ProfileButton />
    </Nav>
  );
};

export default NavSimple;
